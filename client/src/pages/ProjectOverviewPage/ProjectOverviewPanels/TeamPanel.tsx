import Typography from "@mui/material/Typography";
import React from "react";

const TeamPanel = () => {
  return <Typography variant="h2">under development</Typography>;
};

export default TeamPanel;
